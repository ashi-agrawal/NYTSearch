<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.ashiagrawal.nytsearch.activities.WelcomeActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/ivWelcome"
        android:src="@drawable/icon_welcome"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="144dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Search Articles"
        android:id="@+id/btnSearch"
        android:layout_alignLeft="@+id/btnBrowse"
        android:layout_alignStart="@+id/btnBrowse"
        android:onClick="onSearch"
        android:layout_below="@+id/ivWelcome" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Browse Articles"
        android:id="@+id/btnBrowse"
        android:layout_below="@+id/btnSearch"
        android:layout_centerHorizontal="true"
        android:onClick="onBrowse" />

    <ImageView
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:id="@+id/ivSearch"
        android:layout_alignTop="@+id/btnSearch"
        android:layout_toRightOf="@+id/btnSearch"
        android:onClick="onSearchQuestion"
        android:src="@drawable/ic_question"
        android:layout_alignBottom="@+id/btnSearch" />

    <ImageView
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:id="@+id/ivBrowse"
        android:onClick="onBrowseQuestion"
        android:layout_alignTop="@+id/btnBrowse"
        android:layout_toRightOf="@+id/btnBrowse"
        android:src="@drawable/ic_question"
        android:layout_alignBottom="@+id/btnBrowse"
        android:layout_alignLeft="@+id/ivSearch"
        android:layout_alignStart="@+id/ivSearch" />
</RelativeLayout>
